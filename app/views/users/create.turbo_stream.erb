<%= turbo_stream.update "users" do %>
  <%= render @users %>
  <%= paginate @users %>
<% end %>

<%= turbo_stream.update "user-modal-content", partial: "show" %>
<%= turbo_stream.replace "flash", partial: "partials/flash" %>
